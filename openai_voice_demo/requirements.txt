# This folder is an independent install on top of the `voicemem` package.
# Install voicemem itself first, from the repo root:
#   pip install -e .                                       # text-only, base mode (VOICEMEM_AUDIO_NATIVE=false)
#   pip install -e ".[audio,environment,omni,voiceprint]"   # + audio-native perception (VOICEMEM_AUDIO_NATIVE=true)
# Then, from this folder:
#   pip install -r requirements.txt
#
# Realtime mode (providers/realtime.py) uses `client.realtime` (the current,
# non-beta namespace, nested session schema) — verified present in openai==2.33.0
# at the time this was written. If `python -c "from openai import AsyncOpenAI;
# print(hasattr(AsyncOpenAI(api_key='x').realtime, 'connect'))"` prints False on
# whatever version pip resolves, upgrade openai; pipeline mode does not need this.
fastapi
uvicorn[standard]
websockets
openai>=2.33
# No python-dotenv: config.py has its own ~10-line .env loader to avoid the
# extra dependency for something this small.

# backend/local_classifier.py, local_embedder.py, local_emotion_classifier.py:
# local slot classification, memory embedding, and emotion classification --
# replaces voicemem's Classify() LLM call, the OpenAI embeddings API call,
# and an LLM emotion-classification call respectively (see each module for
# real speed/accuracy numbers). All three share one loaded model instance.
# Downloads intfloat/multilingual-e5-small (~450MB) on first run if not
# already cached.
sentence-transformers
# backend/local_asr.py: pipeline mode's local streaming ASR + VAD (replaces
# the OpenAI Realtime transcription session -- see that module for the real
# measured latency reasons and the accuracy trade-off). Model files are NOT
# downloaded automatically: run `bash scripts/download_models.sh
# service/models` from the repo root once (same models service/ uses).
sherpa-onnx
numpy
